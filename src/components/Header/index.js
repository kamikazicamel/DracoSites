import React from 'react';
import { PageHead } from './HeaderElements';

const Header = () => {
    return (
        <PageHead>
            Page Header
        </PageHead>
    );
};

export default Header;
